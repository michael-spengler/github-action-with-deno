name: "create jar file"
on:
  issue_comment:
    types: [created]
jobs:
  distanceCalculator:
    runs-on: ubuntu-latest
    steps:
      - uses: denolib/setup-deno@v2
        with:
         deno-version: v1.x
      - run: |
          deno --version
          ./example-script.bash
